

;要等待此窗口弹出且处于激活状态,等待该窗口变成活动窗口
WinWaitActive("窗口标题"，"窗口文本")

;在选项中(例如Yes/No)中一般有下划线的说明可以用ALT+Y/N，
所以例如用send("!N")选择，！代表ALT

q:
ControlSetText("WinZip 安装程序", "到下列文件夹", "Edit1", "D:\Program Files\WinZip" )
是啥函数？
If...Then表达式，同时还使用了 WinExists 函数，
这段脚本的具体意思是：如果指定窗口存在则关闭该窗口。

尝试再编写一次这个脚本，但不要使用 Send 函数（它会向激活窗口发送键击动作），
试试用 ControlClick 函数吧，这会令脚本更加可靠。

在此之前您或许应该熟读关于 控件（Controls） 的说明。

在脚本中分号 (;) 的身份是注释符
如果某条语句过长则可通过在语句结尾附加一个下划线“_”（称为续行符）从而达到让该语句跨行的目的。


成功：	返回控件标识符（控件ID）。
这个ID是多少？哪里来

对于函数参数可选时，遵循左侧参数必填
输出，在autoit中我只知道用MsgBox，例如（输出绝对值）
$var = Abs(-123.45) ;返回值将是 123.45
MsgBox(0, "绝对值", $var)
还有其他输出方法吗？

变量名前的$符号是变量格式的一部分，是不能够省略的。除此变量名本身的命名规则和一般的编程语言一样，
必须是以英文字母开头的连续的单词，同时变量名中不能出现除了字母、数字和下划线(_)之外的字符。


